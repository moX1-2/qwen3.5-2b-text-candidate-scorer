# 许可与来源范围

JEV 原创代码以 Apache-2.0 发布。checkpoint 作为研究产物公开，不改变 Qwen 基座及八个上游数据源的许可条件。原始题库与转换后的训练题不在公开包中。使用 checkpoint 前请核对上游模型与数据来源的条款。

四个压缩包中的 `adapter_config.json` 与 adapter README 将训练机器的绝对基座路径改为 `models/Qwen3.5-2B-Text`；权重、优化器、评分头和状态文件未改动。
